tags
test.sh
.luarc.json
nvim

spell/

# In your personal fork, you likely want to comment this, since it's recommended to track
# nvim-pack-lock.json in version control - see :help vim.pack-lockfile
# For the official `nvim-lua/kickstart.nvim` git repository, we leave it ignored to avoid unneeded
# merge conflicts.
nvim-pack-lock.json

.DS_Store


# Machine-local configuration (see SECTION 8.6 of init.lua).
# Anything specific to one machine or employer lives here and is never
# committed: file-type associations, house indentation, private DB tooling.
lua/machine.lua
sqlfluff.local.cfg
